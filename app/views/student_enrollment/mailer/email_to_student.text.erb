<%= var(:record).enrollment.student.name %>,

Informamos que seu pedido de inscrição em disciplinas de <%= var(:record).year %>.<%= var(:record).semester %> foi <%= var(:record).created_at == var(:record).updated_at ? 'realizado' : 'atualizado' %>.

<% if var(:new_insertion_requests).any? %>
Foram solicitadas inscrições nas seguintes disciplinas:
<% var(:new_insertion_requests).each do |cer| %>
- <%= cer.course_class.to_label %>
<% end -%>

<% end -%>
<% if var(:remove_removal_requests).any? %>
Foram desfeitas solicitações de remoção das seguintes disciplinas:
<% var(:remove_removal_requests).each do |cer| %>
- <%= cer.course_class.to_label %>
<% end -%>

<% end -%>
<% if var(:existing_insertion_requests).any? %>
As seguintes solicitações de inscrição continuam no pedido:
<% var(:existing_insertion_requests).each do |cer| %>
- <%= cer.course_class.to_label %> (<%= cer.status %>)
<% end -%>

<% end -%>
<% if var(:new_removal_requests).any? %>
Foram solicitadas remoções das seguintes disciplinas:
<% var(:new_removal_requests).each do |cer| %>
- <%= cer.course_class.to_label %>
<% end -%>

<% end -%>
<% if var(:remove_insertion_requests).any? %>
Foram desfeitas solicitações de inscrição nas seguintes disciplinas:
<% var(:remove_insertion_requests).each do |cer| %>
- <%= cer.course_class.to_label %>
<% end -%>

<% end -%>
<% if var(:existing_removal_requests).any? %>
As seguintes solicitações de remoção de disciplinas continuam no pedido:
<% var(:existing_removal_requests).each do |cer| %>
- <%= cer.course_class.to_label %> (<%= cer.status %>)
<% end -%>

<% end -%>

<%= CustomVariable.notification_footer %>