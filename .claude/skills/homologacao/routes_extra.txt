# Rotas HTML alem dos indices: execucao de consulta e telas de registro
# individual. Os ids sao de registros existentes em homologacao.
#
# Os /simulate executam a consulta de verdade (Query.run_read_only_query) e sao
# o UNICO ponto da varredura que exercita o ramo Mysql2. Rodam com
# skip_update: true, entao nao escrevem nem enviam e-mail -- diferente de
# execute_now, que faz as duas coisas.
/notifications/1/simulate
/notifications/2/simulate
/notifications/3/simulate
# Data de consulta que nenhum formato le. A tela deve abrir com aviso e usar a
# data padrao da notificacao -- pagina de erro aqui e regressao.
/notifications/1/simulate?query_params[data_consulta]=31/31/2026
/assertions/1/simulate
/assertions/3/simulate
/students/2129
/enrollments/2903
/course_classes/621
/queries/1
/notifications/1
# Colunas de show que so aparecem na tela do registro, nao no indice.
# Afiliacoes do professor: as colunas sao datetime, nao date -- e o caso em que
# o I18n escolhe o formato longo por extenso se a data nao for convertida antes.
# Este professor tem data de inicio preenchida e data de fim vazia, entao a
# mesma tela exercita o valor presente e o ausente.
/professors/124
# Comentarios da solicitacao, com data e hora.
/enrollment_requests/2850
# Nao adianta acrescentar /admission_applications/<id> para ver o rotulo de
# escolaridade: a secao "Dados" da tela de show carrega sob demanda, e a
# varredura estatica registra so a casca -- 1,4 KB de texto, sem uma data. O
# rotulo com o periodo aparece em /admission_processes/4/complete_pdf, que ja
# esta no routes_binary.txt.
