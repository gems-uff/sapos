<%
  record = var(:record)
  name = record.name
  token = record.token
  process = var(:admission_process).title
  url = var(:admission_apply_url)
%>
<%= name %>,

Sua inscrição no edital de seleção <%= process %> foi realizada com sucesso.

O código de identificação da sua inscrição é:
<%= token %>

Use-o para acompanhar ou alterar alguma informação de sua inscrição no link abaixo:
<%= url %>

<%= CustomVariable.notification_footer %>
