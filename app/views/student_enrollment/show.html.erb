
<div class="landing-main">
  <h2> Matrícula: <%= @enrollment.enrollment_number %> <%= @enrollment.dismissal.nil? ? "" : "(Desligada)" %> </h2>
  <% if @partials.empty? %>
    <p> Bem-vindo ao Sapos. Você ainda não tem acesso a nada nesta matrícula, mas em breve várias funcionalidades interessantes aparecerão aqui para você! </p>
  <% else %>
    <% @partials.each do |partial, locals| %>
      <%= render :partial => partial, :locals => locals %>
    <% end %>
  <% end %>
</div>