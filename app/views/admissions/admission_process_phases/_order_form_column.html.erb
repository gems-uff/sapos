<%= render partial: "application/order_widget", locals: {
  valid_controllers: ["Admissions::AdmissionProcessesController"],
  column: column,
  scope: scope,
  record: record,
  only_value: only_value,
  col_class: col_class
} %>
